{
    "failures": [
        {
            "name": "creatingBlockError",
            "enabled": true,
            "triggers": ["shard == 0 && round == 20"]
        },
        {
            "name": "processingBlockError",
            "enabled": true,
            "triggers": ["shard == 0 && round == 25"]
        },
        {
            "name": "consensusCorruptSignature",
            "enabled": true,
            "triggers": ["round % 11 == 0 && nodeIndex < consensusSize / 2"]
        },
        {
            "name": "consensusV1ReturnErrorInCheckSignaturesValidity",
            "enabled": false,
            "triggers": ["false"]
        },
        {
            "name": "consensusV1DelayBroadcastingFinalBlockAsLeader",
            "enabled": true,
            "comment": "Internally known as 'testnet soft forks'. ???",
            "triggers": ["round % 20 == 0"],
            "parameters": {
                "duration": 12
            }
        },
        {
            "name": "consensusV2CorruptLeaderSignature",
            "enabled": false,
            "triggers": ["round % 13 == 0"]
        },
        {
            "name": "consensusV2DelayLeaderSignature",
            "enabled": false,
            "triggers": ["round % 19 == 0"],
            "parameters": {
                "duration": 4
            }
        },
        {
            "name": "consensusV2SkipSendingBlock",
            "enabled": false,
            "triggers": ["round % 17 == 0 && randomNumber % 2 == 0"]
        }
    ]
}
