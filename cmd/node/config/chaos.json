{
    "failures": [
        {
            "name": "processingTransactionError",
            "enabled": false,
            "triggers": [
                "(nodePublicKeyLastByte % 4 == 0 && transactionHashLastByte % 7 == 0)",
                "round == 500 && randomNumber % 2 == 0"
            ]
        },
        {
            "name": "consensusCorruptSignature",
            "enabled": false,
            "triggers": ["round % 11 == 0"]
        },
        {
            "name": "consensusV1SkipWaitingForSignatures",
            "enabled": false,
            "triggers": ["false"]
        },
        {
            "name": "consensusV1ReturnErrorInCheckSignaturesValidity",
            "enabled": false,
            "triggers": ["false"]
        },
        {
            "name": "consensusV1DelayBroadcastingFinalBlockAsLeader",
            "enabled": false,
            "triggers": ["round % 20 == 0"],
            "parameters": {
                "duration": 12
            }
        },
        {
            "name": "consensusV2CorruptLeaderSignature",
            "enabled": false,
            "triggers": ["round % 13 == 0"]
        },
        {
            "name": "consensusV2DelayLeaderSignature",
            "enabled": false,
            "triggers": ["round % 19 == 0"],
            "parameters": {
                "duration": 4
            }
        },
        {
            "name": "consensusV2SkipSendingBlock",
            "enabled": false,
            "triggers": ["round % 17 == 0 && randomNumber % 2 == 0"]
        }
    ]
}
