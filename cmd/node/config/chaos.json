{
    "failures": [
        {
            "name": "processTransactionShouldReturnError",
            "triggers": [
                "(nodePublicKeyLastByte % 4 == 0 && transactionHashLastByte % 7 == 0)",
                "round == 500 && iAmWaitingNode"
            ]
        },
        {
            "name": "shouldCorruptSignature",
            "triggers": ["round % 11 == 0"]
        },
        {
            "name": "shouldSkipWaitingForSignatures",
            "triggers": ["false"]
        },
        {
            "name": "shouldReturnErrorInCheckSignaturesValidity",
            "triggers": ["false"]
        },
        {
            "name": "shouldCorruptLeaderSignature",
            "triggers": ["round % 13 == 0"]
        },
        {
            "name": "shouldSkipSendingBlock",
            "triggers": ["round % 17 == 0 && randomNumber % 2 == 0"]
        }
    ]
}
