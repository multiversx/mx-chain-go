{
    "failures": [
        {
            "name": "processTransactionShouldReturnError",
            "when": "nodePublicKeyLastByte % 4 == 0 && transactionHashLastByte % 7 == 0"
        },
        {
            "name": "maybeCorruptSignature",
            "when": ""
        }
    ]
}
