name: Self hosted example

on:
    pull_request:
    workflow_dispatch:

jobs:
    build:
        runs-on: self-hosted
        steps:
            - name: Test
              run: |
                  echo "Hello, world!"
